<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Error</title>
    <style>
      body { font-family: Arial, sans-serif; background: #0f0f0f; color: #f2f2f2; margin: 0; }
      .wrap { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 24px; }
      .card { max-width: 560px; width: 100%; background: #171717; border: 1px solid #2a2a2a; border-radius: 12px; padding: 24px; }
      h1 { font-size: 20px; margin: 0 0 8px; }
      p { margin: 0 0 12px; color: #cfcfcf; }
      a { color: #caa9ff; }
      code { color: #9fd2ff; }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="card">
        <h1><%= message || 'Something went wrong' %></h1>
        <p>Status: <%= error && error.status ? error.status : 500 %></p>
        <% if (error && error.stack) { %>
          <p><code><%= error.stack %></code></p>
        <% } %>
        <p><a href="/">Back to home</a></p>
      </div>
    </div>
  </body>
</html>
